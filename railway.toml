[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build && php artisan route:clear && php artisan view:clear && php artisan config:clear"

[deploy]
  startCommand = "php artisan config:cache && php artisan serve --host=0.0.0.0 --port $PORT"

[variables]
  APP_DEBUG = "true"
